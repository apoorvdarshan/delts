For every code change, always create a git commit and push it to the current branch before finishing, unless the user explicitly says not to or pushing is impossible.
Do not add co-author or Co-authored-by trailers to commits.

iOS build verification:
- Do not use the iOS Simulator unless the user explicitly asks for it.
- After each iOS code change, build, install, and launch the app on Apoorv's iPhone so the user can inspect the result on device.
